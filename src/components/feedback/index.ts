/**
 * Fachada do módulo de feedback global (core / Módulo 1).
 * Consumir sempre por aqui: import { GlobalLoadingIndicator } from '@/components/feedback'
 */
export { GlobalLoadingIndicator } from './GlobalLoadingIndicator'
export { useGlobalBusy } from './useGlobalBusy'
export type { GlobalBusyState } from './useGlobalBusy'
